<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto"
    package="com.acando.notegen">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:resizeableActivity="true"
        android:theme="@style/AppTheme">

        <provider
            android:name="com.acando.notegen.api.NoteContentProvider"
            android:authorities="com.acando.notegen.api.NoteContentProvider"
            android:exported="false" />

        <activity
            android:name="com.acando.notegen.NoteListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:label=""
            android:name="com.acando.notegen.DetailActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:label="Labels"
            android:name="com.acando.notegen.LabelListActivity"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>