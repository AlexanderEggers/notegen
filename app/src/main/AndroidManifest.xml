<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.acando.todohelper">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <provider
            android:name=".api.ToDoContentProvider"
            android:authorities="com.acando.todohelper.api.ToDoContentProvider"
            android:exported="false" />

        <activity
            android:name=".internal.ToDoActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:label=""
            android:name=".internal.DetailActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <service android:name=".internal.SyncService" android:exported="false" />

    </application>

</manifest>